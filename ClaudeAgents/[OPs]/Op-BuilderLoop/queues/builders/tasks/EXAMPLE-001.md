# EXAMPLE-001 — Example Task

## Objective
Add input validation to `parse_config()` in `config.py`. Reject empty strings and non-dict inputs.

## Source Paths
- src/config.py

## Deliverables
- `parse_config()` raises `ValueError` on invalid input
- No changes to function signature

## Forbidden
- Do not modify any other functions
- Do not add new dependencies
